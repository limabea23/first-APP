import "../styles/globals.css";
			export const metadata = {
				title: "PedriNation",
			};
			export default function RootLayout({ children }) {
				return (
					<html>
						<body>{children}</body>
					</html>
);
	}